export interface CreatePupilyPayload {
    userName: string;
    password: string;
}

export interface EditPupilyPayload {
    userName: string;
}

export interface LoginPupilyPayload {
    userName: string;
    password: string;
}
